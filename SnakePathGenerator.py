class PathGenerator:
    def __init__(self):
        pass
    def GenFoodPath(self):
        #Initial Snake Position
        #Snake Trace
        #GenPath(InitSnakePs, FoodPs)
        #GenPath(NewSnakePs, TailPs):
            #if True return the path generate by GenPath(food)
            #if False return False
        pass
    def GenTailPath(self):
        #Initial Snake Positioin
        #Snake Trace
        #return GenPath(InitSnakePs, TailPs)
        pass

    def GenPath(self, isBool = False):
        #Do the Calculation:
        #if can generate the path
            #if isBool == False:
                #Return PATH
            #else:
                #Return True
        #else
            #Return False
        pass
